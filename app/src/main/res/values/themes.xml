<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Grinnet" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.Grinnet" parent="Base.Theme.Grinnet" />

    <style name="SplashTheme" parent="Theme.SplashScreen">
        <item name="android:windowSplashScreenAnimatedIcon">@mipmap/logo_splash</item>
        <item name="postSplashScreenTheme">@style/Theme.Grinnet</item>
        <item name="android:windowSplashScreenAnimationDuration">600</item>
    </style>
</resources>
